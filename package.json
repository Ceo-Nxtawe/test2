{
  "name": "solana-monitor-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "dependencies": {
    "@solana/web3.js": "^1.91.1",       // Bibliothèque pour interagir avec Solana
    "@solana/spl-token": "^0.4.1",      // Gestion des tokens SPL
    "cors": "^2.8.5",                   // Gestion des politiques CORS
    "dotenv": "^16.4.5",                // Chargement des variables d'environnement
    "express": "^4.18.3",               // Framework HTTP
    "ws": "^8.13.0"                     // Gestion des connexions WebSocket
  },
  "devDependencies": {
    "nodemon": "^3.1.0"                 // Rechargement automatique en développement
  },
  "engines": {
    "node": ">=18"                      // Assure que Node.js 18+ est utilisé
  }
}
